"use client";

import ViewContentPixel from "@/components/meta/ViewContentPixel";

export default function ProductViewTracker({ product }: { product: any }) {
  return <ViewContentPixel product={product} />;
}
